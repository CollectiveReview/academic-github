import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Academic GitHub | Homepage",
  description: "",
};

const Page = () => {
  return (
    <div>
      This is a Home Page
    </div>
  );
};

export default Page;
